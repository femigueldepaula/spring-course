spring:
  application:
    name: spring-course
  profiles:
    active: dev
  datasource:
    driver-class-name: "com.mysql.cj.jdbc.Driver"
  jpa:
    hibernate:
      ddl-auto: update
  jackson:
    default-property-inclusion: non-null
logging:
  file: logs/spring-boot-elk.log